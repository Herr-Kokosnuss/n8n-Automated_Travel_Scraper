{
  "name": "travel-scrap",
  "version": "1.0.0",
  "description": "scrap travel deals and travel trends",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "npm install --omit=dev",
    "heroku-postbuild": "npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.1.0",
    "express": "^5.1.0",
    "puppeteer": "^24.12.1",
    "puppeteer-real-browser": "^1.4.2"
  }
}
